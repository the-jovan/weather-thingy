import { FunctionComponent } from "react";
import classes from "./footer.module.scss";

const Footer: FunctionComponent = () => {
  return <footer className={classes.footer}>Weather Thingy</footer>;
};

export default Footer;
